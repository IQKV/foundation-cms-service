# Cms Environment Variables Template
# Copy this file to .env.local, .env.uat, or .env.prd and configure values

# =============================================================================
# Spring Profile Configuration
# =============================================================================
SPRING_PROFILES_ACTIVE=local

# =============================================================================
# Database Configuration
# =============================================================================
DB_HOST=localhost
DB_PORT=5432
DB_NAME=cmsservice
DB_USERNAME=svc_cms_dba
DB_PASSWORD=svc_cms_dba

# =============================================================================
# RabbitMQ Messaging Configuration
# =============================================================================
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USERNAME=svc_cms_rmq
RABBITMQ_PASSWORD=svc_cms_rmq

# =============================================================================
# Security / JWT
# In deployed (K8s) environments, set JWT_JWKS_URI so the service fetches the
# IAM public key dynamically — no mounted key copies, transparent key rotation.
# Leave JWT_JWKS_URI unset for local dev; the classpath PEM fallback is used.
# =============================================================================
# JWT_JWKS_URI=http://foundation-iam-service/.well-known/jwks.json
# JWT_PUBLIC_KEY_PATH=classpath:keys/public.pem

# =============================================================================
# Platform / Tenancy
# =============================================================================
ROLLOUT_MODE=MULTI_TENANT
DEFAULT_TENANT_KEY=platform
DEFAULT_TENANT_NAME=Acme Corp.

# =============================================================================
# Billing Service Integration
# =============================================================================
BILLING_SERVICE_URI=http://foundation-billing-service
BILLING_PLAN_REFRESH_INTERVAL=PT10M

# =============================================================================
# Mail Configuration (MailHog — used by Docker Compose only)
# =============================================================================
MAIL_HOST=localhost
MAIL_PORT=1025
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_FROM=noreply@iqkv.dev
MAIL_REPLY_TO=

# =============================================================================
# PostgreSQL container vars (used by postgres image directly)
# =============================================================================
POSTGRES_DB=cmsservice
POSTGRES_USER=svc_cms_dba
POSTGRES_PASSWORD=svc_cms_dba
